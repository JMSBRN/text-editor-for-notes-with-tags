import { Paper } from '@mui/material';
import { styled } from 'styled-components';

const NoteStyled = styled(Paper)`
  margin: 5px auto;
  width: 600px;
  height: 80px;
  display: flex;
  align-items: center;
  justify-content: space-around;
`;

export default NoteStyled;
